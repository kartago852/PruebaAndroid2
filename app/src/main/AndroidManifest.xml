<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pruebaandroid2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Views.dashboard"></activity>
        <activity android:name=".Views.vistaAlternativas" />
        <activity android:name=".Views.home" />
        <activity android:name=".Views.registerAdmin" />
        <activity android:name=".Views.registerProvider" />
        <activity android:name=".Views.register" />
        <activity android:name=".Views.MainActivity" />
        <activity
            android:name=".Views.splashscreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>